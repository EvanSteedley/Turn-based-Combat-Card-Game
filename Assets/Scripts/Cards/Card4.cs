using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Card4 : Card
{
    // Start is called before the first frame update
    void Start()
    {
        cardName = "Card4";
        id = 4;
        mana = 5;
        damage = 0; //will icrease max mana by 2
    }

    // Update is called once per frame
    void Update()
    {

    }
}
