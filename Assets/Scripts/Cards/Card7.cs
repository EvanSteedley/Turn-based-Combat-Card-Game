using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Card7 : Card
{
    // Start is called before the first frame update
    void Start()
    {
        cardName = "Card7";
        id = 7;
        mana = 8;
        damage = 0; //will icrease max hp 25
    }

    // Update is called once per frame
    void Update()
    {

    }
}
